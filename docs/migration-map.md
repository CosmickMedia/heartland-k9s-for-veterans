# Migration map — every live URL and where it goes

Source: https://heartlandk9s.org/ (WordPress 7.1 + Avada/Fusion Builder, 40 published pages, 0 posts, 244 media, 2 FooGalleries). Discovery data: `discovery/live/inventory.json`, `discovery/live/media-manifest.json`, `discovery/live/galleries.json`.

How the migrated pages were produced:

- `tools/fusion-to-blocks.mjs` converts the rendered Fusion markup of each live page into clean core block markup (headings, paragraphs, lists, separators, image/gallery/file/video/buttons blocks) with import tokens (`{{media:live:media:<id>}}`, `{{post_url:live:page:<id>}}` …). Inline styles, Fusion classes, animation attributes, `[ccf_form]`/`[fusion_*]` shortcodes, empty `<h1>` spacers and Figma paste comments are stripped. The original rendered HTML is kept for traceability in `payload-src/source/live__page__<id>.html` (gitignored) next to the full uncurated conversion (`…blocks.html`); the curated content that a page record actually attaches lives in `payload-src/content/live__page__<id>.html`. Per-page curation rules (what moved into template sections, what was removed) are the `PAGE_RULES` table inside the converter and are listed in the Notes column below.
- `tools/extract-registry.mjs` turns the 15 BarKode record pages + the master template into `hk9_barkode` records (`payload-src/records/barkode/`, gitignored because they carry personal contact details).
- Page records: `payload-src/records/pages/live-*.json` (migrated live pages) and `ref-*.json` / `new-*.json` (reference-design pages, authored separately).
- Redirect rules: `payload-src/records/redirects.json` (seeded into the plugin's `hk9_redirects` option; 301 unless stated).

Legend — Disposition: **Migrated** (page kept at the same URL, converted to blocks + template sections), **Decomposed** (page content became CPT records; the page keeps its URL as a listing), **Replaced** (URL kept, content replaced by the reference-design page), **Draft** (imported unpublished for traceability), **Registry** (BarKode record; canonical URL moves under `/barkode/`), **Redirect** (URL only exists as a redirect).

## Pages (40)

| Live URL | Live title (id) | Disposition | New URL | Template / type | Redirect | Notes (text changes are listed exhaustively) |
|---|---|---|---|---|---|---|
| `/` | HOME (6) | Replaced | `/` | `home.php` sections (`ref:page:home`, front page via Reading settings) | — | Reference homepage with live copy corrections (see `docs/conflict-log.md`). Live Fusion markup converted for reference only (`source/live__page__6.blocks.html`); the self-hosted video (media 2881) converts to a `core/video` block if wanted. |
| `/5-questions/` | 5 Questions To Ask Before Partnering With a Service Dog (16) | Migrated | `/5-questions/` | `landing.php` (`live:page:16`) | — | Content verbatim. Dropped the in-content H1 duplicating the page title (hero renders it). Kept the "Learn More" attribution link to anythingpawsable.com (new tab). The closing "Still think a Service Dog is for you?" heading + "Submit an Application" button moved into the CTA band → `/online-application/`. Hero band shows the title only (hero text and excerpt are empty so the opening paragraph is not rendered twice — earlier builds repeated the first sentence plus a shortened paraphrase in the hero). |
| `/online-application/` | Initial Online Application (1989) | Migrated (form rebuilt) | `/online-application/` | `application.php` (`live:page:1989`) | — | Intro ("Are You Ready" copy) kept verbatim as block content; "Before you apply please read 5 Questions…" link → `/5-questions/`. The dead `[ccf_form id="2156"]` shortcode was removed; the form is the plugin's Initial Application Inquiry (definition of the original form is unrecoverable — stated in the form notice). Success page → `/thank-you/`. |
| `/contact/` | Contact (2032) | Replaced | `/contact/` | `contact.php` sections (`ref:page:contact`) | — | Live page = dead `[ccf_form id="2132"]` + address/phones/Facebook; all facts carried into settings and the reference contact page. Every in-content link to `/contact/` on migrated pages resolves to `{{post_url:ref:page:contact}}`. |
| `/service-dogs-and-the-ada/` | Service Dogs and the ADA (1995) | Migrated | `/service-dogs-and-the-ada/` | `landing.php` (`live:page:1995`) | — | **Link fix:** "Frequently Asked Questions" button pointed at `https://archive.ada.gov/` → now `https://www.ada.gov/resources/service-animals-faqs/`. The 4 DOJ Q/A items populate the landing `faq` section (questions sentence-cased, the "QUESTION:" prefix removed; answers verbatim; source note names the DOJ). Remaining text (DOJ "Questions about Service Animals and the ADA" intro + ada.gov 2010 link, Fair Housing Act section + HUD PDF button) stays as block content, so on the new page the FAQ follows the content instead of preceding it. The long intro `<h3>` became a paragraph. |
| `/privacy-policy/` | Privacy Policy (3) | Migrated | `/privacy-policy/` | default page (`live:page:3`) | — | Content verbatim (7 headings). **Flagged for client review:** the 2020 policy refers to "the form on our contact page" and to advertising partners / Google ad cookies (opt-out link `http://www.google.com/privacy_ads.html`), while the site now runs Fathom analytics and a cookie-notice banner; effective date "November, 19, 2020" retained. |
| `/donate/` | Donate (2120) | Migrated (sections) | `/donate/` | `donate.php` (`live:page:2120`) | — | `options` section: Zeffy (logo media 3718, primary) → zeffy.com form; PayPal hosted button `<form>` → GET link `https://www.paypal.com/cgi-bin/webscr?cmd=_s-xclick&hosted_button_id=METNHXQ9EBUMQ` labelled "Donate with PayPal"; Amazon wishlist (logo media 3678) → `https://www.amazon.com/hz/wishlist/ls/35NF4IVCKN76G?ref_=wl_share`; Mail-in item + `mail_in` section using the settings address (12651 Gateway Dr, Neosho MO 64850). `tax` section = live EIN statement verbatim except **typo fix** "to the extend allowed by law" → "extent". Zeffy logo alt (file-name string) blanked. No block content attached (everything lives in sections). Option-card copy: `options.intro` joins two live sentences ("Donating has never been easier!" + "Simply click on whatever platform you wish to use for your contribution.") so the live "Donating has never been easier!" (three times on live) renders once; Zeffy card "Click the Zeffy logo to donate." = live "Click the Zeffy Logo to Donate!" sentence-cased; PayPal card "Donate online with PayPal." verbatim; Amazon card "Click through to HK9’s Daily Needs Wishlist on Amazon." paraphrases live "Click the Amazon Picture to be taken to Hk9s Daily Needs Wishlist!"; mail card blurb "Prefer to send a check? Mail your donation to the address below." is UI copy. `mail_in` text = the live "Thank you so much for donating! We will put your gift to work right away." + phone line verbatim. Hero text/excerpt = the tax statement's second sentence (comma added after "Veterans"), so that sentence also appears in the `tax` section. |
| `/back-the-pack/` | Back the Pack (2072) | Decomposed | `/back-the-pack/` | `partners.php` (`live:page:2072`) | — | 13 FooGallery 2094 logos → `hk9_partner` records (type `back-the-pack`), rendered by the `logos` section (mode auto). Intro sentence kept in `logos.intro` and the CTA band; **typo fix** "If you would like the join the pack" → "to join the pack". Buttons: Contact Us → `/contact/`, View Campaigns → `/campaigns/`. |
| `/volunteer/` | Volunteer (2124) | Migrated | `/volunteer/` | `landing.php` (`live:page:2124`) | — | Content verbatim incl. the "Paws for Training" paragraph and both photos (media 2803, 2798; alts empty on live). "Contact Us" button moved into the CTA band. |
| `/photos/` | Photos (2455) | Migrated | `/photos/` | `gallery.php` (`live:page:2455`) | `/?foogallery=2468` → `/photos/` | FooGallery 2468 → one `core/gallery` block of the same 71 attachments in live order, linked to the originals, with the 2 live captions ("Time To Work", "Hannibal visits Arkansas Veterans" + descriptions). `gallery_options`: lightbox on, 3 columns, captions on. Featured image 2456 kept. |
| `/events/` | Events (2483) | Decomposed | `/events/` | `events.php` (`live:page:2483`) | — | The single event (Joplin Jailbirds Veteran Appreciation Night, 25 Jul 2026, past) → `hk9_event` record (flyer media 3777, SimpleTix link). Page = upcoming/past lists + CTA ("We are actively looking for sponsors for this event." generalised to "…for our events."). No block content attached. |
| `/success/` | Success (2491) | Draft | — | default page, `status: draft` (`live:page:2491`) | `/success/` → `/stories/` | Placeholder cards linking to the sample story; converted verbatim for traceability only (3 stock images, "Read More" buttons → `/stories/`). |
| `/success/sample-success-story/` | Sample-Success-Story (2492) | Draft | — | default page, `status: draft`, parent 2491 (`live:page:2492`) | `/success/sample-success-story/` → `/stories/` | Template sample ("Snippet About the breeder" …); content boxes → image + heading + paragraph; image carousel → gallery. Traceability only. |
| `/thank-you/` | Thank You (2505) | Migrated | `/thank-you/` | `landing.php` (`live:page:2505`) | — | Application success page. **Stale address replaced:** "PO Box 101, Saginaw, MO 64864" → "12651 Gateway Dr, Neosho, MO 64850" (recorded in `docs/conflict-log.md`). Medical-history PDF (media 2506) kept as a `core/file` block; heading "Click The Image to Print or Download the Medical History Form" → "Print or Download the Medical History Form" (no image to click). Dropped the duplicate "Thank You!" heading and an empty `<h1>` spacer; Figma paste comment removed. Hero band shows the title only (hero text and excerpt empty — the "Please download or print…" sentence is rendered once, in the content, where it leads into the mailing address). |
| `/campaigns/` | CAMPAIGNS (2800) | Decomposed | `/campaigns/` | `campaigns.php` (`live:page:2800`) | — | 5 campaign blocks → `hk9_campaign` records (sponsor logos → partners); page = list (mode auto) + CTA "Sign us up! Let's change a life!" → `/contact/`. Title case "CAMPAIGNS" → "Campaigns". Five empty `<h1>` spacers dropped. No block content attached. **Edits inside the campaign records:** "Paws" for Giving — **stale text removed** "The stickers we have on order are 4\"x4\" and will be available by 2023!" → "The stickers are 4\"x4\"." (body) / "The 4\"x4\" stickers are no cost to your business." (summary); Heartland Ambassador — **typo fix** "Educate you patrons" → "Educate your patrons"; campaign summaries (`hk9_summary`) and the Dog House / One of Us / Paws for Giving excerpts are condensed from each campaign's opening sentences (not verbatim); the Coloring Book and Ambassador excerpts are live lines. **UI copy:** the page CTA text "Contact us to learn more about our campaigns and join the Heartland Team!" paraphrases the One of Us line "Contact us by clicking the button below to learn more about this program and join the Heartland Team!" (which stays verbatim in that record). The sponsor-logo strips under both "Thank you for participating…" headings render from the `hk9_sponsors` relationship; no sentence was added there (a previously authored line "We are grateful to the businesses participating in this campaign." was removed). |
| `/meet-the-team/` | Meet the Team (2856) | Decomposed | `/meet-the-team/` | `people.php` (`live:page:2856`) | — | 11 profiles → `hk9_person` records (the one personal non-org email is withheld). Page = grid (mode auto). Facebook-pasted `<div>` trees and file-name alt texts cleaned. No block content attached. **Typo fixes in bios:** "Human- Animal bond" → "Human-Animal bond" (Chris Flemming); "and now work in the tech industry" → "and now works in the tech industry" (Cody Simmons). Hard line breaks inside Cody's and Ted's bios joined into sentences. |
| `/barkode/` | BarKode – A Service of Heartland Canines for Veterans (2944) | Replaced | `/barkode/` | `barkode.php` sections (`ref:page:barkode`) | `/master_template_barkode/` → `/barkode/` | Live copy (voluntary service, QR + team number, free for HK9 teams, evaluation + annual fee for outside teams, Derron & Rosie dedication) reused in the reference BarKode page. |
| `/hk9-current-teams-in-training/` | HK9 Teams in Training (3011) | Decomposed | `/hk9-current-teams-in-training/` | `teams.php` (`live:page:3011`) | — | 2 teams → `hk9_team` records (`live:team:billy-and-caddie`, `live:team:marilyn-and-travis`, status in-training, donate links → `/donate/`). Page = list (status in-training) + CTA "Help our teams in training" → `/donate/`. **Copy edit:** Marilyn & Travis "alllll obedience training" → "all obedience training" (summary, excerpt and body). Hero/CTA copy is short UI text (the live page had no intro). |
| `/our-highlighted-team/` | Our Highlighted Team (3496) | Decomposed | `/our-highlighted-team/` | `highlighted-team.php` (`live:page:3496`) | — | `team` section → `{{post:live:team:billy-and-caddie}}` (the live page duplicated the Billy & Caddie profile). |
| `/the-service-k9-program/` | The Service K9 Program (3084) | Migrated | `/the-service-k9-program/` | `landing.php` (`live:page:3084`) | — | Content verbatim incl. the accept/reject criteria (the live "10 weeks and 16 months" vs "older than 14 months" wording is kept as-is and flagged in `docs/conflict-log.md`). "Does your K9 meet the requirements…" paragraph + "Contact Us" button moved into the CTA band. Empty `<h1>` spacer dropped. |
| `/how-it-works-veteran-consideration/` | How it Works (Veteran Consideration) (3093) | Migrated | `/how-it-works-veteran-consideration/` | `landing.php` (`live:page:3093`) | — | Content verbatim. **Typo fix:** "guidlines" → "guidelines". Kept verbatim on purpose: "HK9 does not provided funding…" (flagged, not corrected). Three buttons (Donate, "5 Questions", Online Application) kept as one buttons block. Hero band shows the title only (hero text and excerpt empty — "Thank you for considering HK9…" is rendered once, in the content). |
| `/heartland-gear/` | Heartland Gear (3408) | Migrated | `/heartland-gear/` | `landing.php` (`live:page:3408`) | — | **Removed stale text:** the two PayPal ordering paragraphs ("Thank you for helping HK9! To order, simply click the button below! Your donation will go through paypal…" and "Not local to the 4 state area? Please add $5 for shipping…") — orders now go through Links Ink. **Typo fix:** "spread the world about HK9" → "spread the word about HK9". "BROWSE NOW" → `https://linksinkllc.com/?s=Heartland` kept as the CTA band button. Links Ink logo (media 3760) + partner paragraph kept. |
| `/the-hk9-coloring-book/` | The Hk9 Coloring Book (3627) | Migrated | `/the-hk9-coloring-book/` | `landing.php` (`live:page:3627`) | — | Title case "The Hk9 Coloring Book" → "The HK9 Coloring Book". Content = cover image, "More than a coloring book." intro, classroom photo, "Check them out!" + the 3 sample pages (merged into one 3-column gallery). The 8 sponsor tiers (Bronze $500 / Silver $1,000 / Gold $4,000 / Platinum $10,000 / Page Partner $2,500 / Featured Page Sponsor $4,000 / Legacy Page Sponsor $7,500 (+ second page $2,000) / Classroom Sponsorship $300) with their benefit lines populate the `tiers` section (benefit bullets sentence-cased; Platinum marked highlight). "Sign us up! Let's change a life!" / "Sponsor a class!" → `/contact/` (tier buttons + CTA band). `tiers.intro` = the live "This educational coloring book will be featured…" sentence followed by the two live tier-group sentences verbatim ("In THIS sponsorship, these books are YOURS for distribution!" and "This is for dedicated PAGE sponsor levels that can be included in our coloring books."), because the theme renders one intro above all eight tiers; the live page repeated the first sentence above each group. **Typo fix:** Platinum "highlighted page in that includes maximum exposure" → "Highlighted page that includes maximum exposure". Classroom Sponsorship keeps the live closing "Sign up now!". CTA band = live heading only (no body text). **Alt text authored** (media alts were the file names "Cover"/"Dogclass"): cover → "Cover of the Heartland Canines for Veterans Kids’ Coloring & Activity Book", classroom image → "Illustration of a service dog sitting beside a student in a classroom"; the three sample pages have empty alts. Hero band shows the title only (hero text and excerpt empty — "Help fund service dogs…" is rendered once, under "More than a coloring book."). |
| `/heartland-obedience-training-2/` | Heartland Obedience Training (3742) | Migrated | `/heartland-obedience-training-2/` | `landing.php` (`live:page:3742`) | — | Slug kept incl. the "-2" suffix (QR/links may point at it). Content verbatim; the price sheet `.docx` (media 3745) text was extractable (`word/document.xml`) and is added as headings + lists (Farm training $60/$50/$75 per hour, travel-to-you +$25 within 25 mi of Oronogo / +$1 per mile beyond, 3-session packages $150 farm / $200 travel) followed by a `core/file` download block. "Interested in having your dog trained?" + "Contact Us" moved into the CTA band; "Check out A Dog's Dream" (addadoption.org) button kept in content. |
| `/larry-and-archie-service-k9/` | Larry and Archie (Service K9) (2910) | Registry | `/barkode/larry-and-archie-service-k9/` | `hk9_barkode` (`live:barkode:2910`) | `/larry-and-archie-service-k9/` → record | Image-only record (K9 photo + "ID Back" card image → `id_card_images`); registry ID taken from the photo title; text fields empty pending client review. |
| `/jimmy-and-riley-service-k9/` | Jimmy and Riley (Service K9) (2939) | Registry | `/barkode/jimmy-and-riley-service-k9/` | `hk9_barkode` (`live:barkode:2939`) | legacy path → record | Handler phone number moved to the admin-only review notes (not rendered). No breed / contact line on the live page. |
| `/vern-and-bella-service-dog/` | Vern and Bella (Service Dog) (2979) | Registry | `/barkode/vern-and-bella-service-dog/` | `hk9_barkode` (`live:barkode:2979`) | legacy path → record | Certification line "(In training)" kept verbatim. |
| `/hk923004/` | Alex and Sammi (Service Dog) (2993) | Registry | `/barkode/hk923004/` | `hk9_barkode` (`live:barkode:2993`) | legacy path → record | Registry ID inferred from the slug as HK923-004 (review note). |
| `/hk923-005/` | Jennifer and Toad (Service K9) (2998) | Registry | `/barkode/hk923-005/` | `hk9_barkode` (`live:barkode:2998`) | legacy path → record | Veterinary clinic street address kept inside the vet-contact field (business address, as on the live record). |
| `/madison-and-gunther-service-k9/` | Madison and Gunther (Service K9) (3046) | Registry | `/barkode/madison-and-gunther-service-k9/` | `hk9_barkode` (`live:barkode:3046`) | legacy path → record | Colon-style labels ("Handler:", "Emergency Vet Contact:") parsed like the dash style. |
| `/scott-and-elke-service-k9/` | Scott and Elke (Service K9) (3386) | Registry | `/barkode/scott-and-elke-service-k9/` | `hk9_barkode` (`live:barkode:3386`) | legacy path → record | Only record with an explicit "Service Number Identification" line (Hk9-2402); no veterinary contact on live. |
| `/jeremy-and-nova-service-k9/` | Jeremy and Nova (Service K9) (3428) | Registry | `/barkode/jeremy-and-nova-service-k9/` | `hk9_barkode` (`live:barkode:3428`) | legacy path → record | No registry ID on live. |
| `/paul-and-mj-service-k9/` | Paul and MJ (Service K9) (3484) | Registry | `/barkode/paul-and-mj-service-k9/` | `hk9_barkode` (`live:barkode:3484`) | legacy path → record | No registry ID on live. |
| `/cody-and-willow-service-k9/` | Cody and Willow (Service K9) (3597) | Registry | `/barkode/cody-and-willow-service-k9/` | `hk9_barkode` (`live:barkode:3597`) | legacy path → record | No registry ID on live. |
| `/master_template_barkode/` | Barkode Master Template (Service K9) (3673) | Draft (Registry) | — | `hk9_barkode`, `status: draft`, title "BarKode Master Template (example)" (`live:barkode:3673`) | `/master_template_barkode/` → `/barkode/` | Placeholder values ("Dog Name", "Breed"); imported as an editor example only. |
| `/barkode-mosby-hk9t26-01/` | Mosby (Therapy K9) (3675) | Registry | `/barkode/barkode-mosby-hk9t26-01/` | `hk9_barkode` (`live:barkode:3675`) | legacy path → record | "MOSBY is an ACTIVE traveler, he knows his way home." kept as the record notice; the home street address that followed it is withheld (review notes). Stray "(" in the certification line removed. |
| `/kimber_hk92026-01/` | Kimber (Service K9 In Training) (3680) | Registry | `/barkode/kimber_hk92026-01/` | `hk9_barkode` (`live:barkode:3680`) | legacy path → record | program_type in-training; ID from slug. |
| `/caddie-service-k9_hk92026-02/` | Caddie (Service K9 In Training) (3683) | Registry | `/barkode/caddie-service-k9_hk92026-02/` | `hk9_barkode` (`live:barkode:3683`) | legacy path → record | program_type in-training; ID from slug; same K9 as team `live:team:billy-and-caddie`. |
| `/tex-service-k9-hk926-002/` | Tex (Service K9) HK926-002 & James Lange (3686) | Registry | `/barkode/tex-service-k9-hk926-002/` | `hk9_barkode` (`live:barkode:3686`) | legacy path → record | Handler name removed from the record title (title = "Tex (Service K9) HK926-002"); handler phone moved to review notes. |
| `/sandy-therapy-k9t26-02/` | Sandy (Therapy K9) (3690) | Registry | `/barkode/sandy-therapy-k9t26-02/` | `hk9_barkode` (`live:barkode:3690`) | legacy path → record | ID copied from the slug as "K9T26-02" (other therapy records use HK9T…; review note). |

Registry rows: all 15 real records import `status: publish`, `sensitive: true`, `hk9_legacy_path` = the live path, featured image = the K9 photo; emergency-contact and veterinary-contact lines are stored verbatim (registry purpose); handler phone numbers, the one residential address and any unclassifiable line go to `_hk9_review_notes` ("pending client review, not rendered"). Record titles are "<Dog> (<Program type>) <ID when known>" — handler names never appear in titles. The CPT is `noindex`, excluded from search/REST/sitemaps/feeds.

## Other live URLs

| Live URL | What it is | Disposition | New URL / behaviour | Redirect |
|---|---|---|---|---|
| `/slide/heartland-hero/` | Avada slider CPT (in live sitemap) | Redirect | `/` | 301 (`redirects.json`) |
| `/slide-page/homepage-fusion/` | Avada slider taxonomy term (in live sitemap) | Redirect | `/` | 301 (`redirects.json`) |
| `/tag/poker-run/` | post_tag "Poker Run" (empty archive, 200 on live) | Kept | `/tag/poker-run/` — tag term created (`records/terms.json`), renders the empty archive template (200) | — |
| `/tag/veterans-day/` | post_tag "Veterans Day" (empty archive) | Kept | `/tag/veterans-day/` — same as above | — |
| `/author/admin/`, `/author/hk9director/` | Author archives — the live site 301s both to `/` (Screaming Frog crawl, 2026-09) | Redirect | `/` — seeded by the plugin (`Redirects\Store::seeds()`) and carried by `redirects.json`; the resolver runs on `parse_request` before the privacy module's author guard, so the 301 wins; `?author=N` stays a 404 and the public `/wp-json/wp/v2/users` listing stays disabled (see “URL coverage” below) | 301 (seed + `redirects.json`) |
| `/?foogallery=2468` | FooGallery "Heartland Canines for Veterans Image Gallery" (71 images) | Redirect | `/photos/` (gallery block) | 301 (`redirects.json`) |
| `/?foogallery=back-the-pack-partners` | FooGallery "Back the Pack Partners" (13 logos) | Redirect | `/back-the-pack/` (partner records) | 301 (`redirects.json`) |
| `/sitemap.xml` + `sitemap-post-type-page.xml`, `sitemap-post-type-slide.xml`, `sitemap-taxonomy-post_tag.xml`, `sitemap-taxonomy-slide-page.xml` | Slim SEO sitemaps | Replaced | Core `/wp-sitemap.xml` (pages, posts, CPTs with `show_in_rest`; BarKode records excluded) | — |
| `/wp-content/uploads/<year>/<month>/<file>` | 244 media originals (incl. 5 PDFs, 1 docx, 1 mp4) | Migrated | **Live site (adoption mode, `install.md` §B): every path stays identical** — the content-only payload binds each `live:media:<id>` to the attachment that already holds that id/file and never copies, renames or deletes anything under `uploads/`; `-scaled` and `-WxH` renditions stay next to the originals (see “URL coverage” below). Only on a *fresh* site with the full payload does WordPress re-file the uploads (every in-content reference uses `{{media_url:…}}` tokens, so links stay valid there too; old deep links are then not redirected). | — |
| `/<page>/<attachment-slug>/` (e.g. `/home/cq-vet-service-dogs/`) | Attachment pages | Not preserved | WordPress 7.1 disables attachment pages by default (`wp_attachment_pages_enabled=0`); the file itself is served from its new uploads URL | — |
| `/feed/`, `/comments/feed/` | Feeds (empty; 0 posts) | Kept (core) | Same URLs, populated by blog posts | — |
| `/wp-json/wp/v2/pages`, `/wp-json/wp/v2/media`, … | REST API (public on live, exposes registry records + users) | Kept, hardened | Registry CPT not in REST; user enumeration disabled | — |

## Cross-cutting decisions recorded here

- **Internal links** inside migrated content are rewritten to tokens by path (`discovery/live/inventory.json`): `/contact/` → `ref:page:contact`, `/` → `ref:page:home`, `/barkode/` → `ref:page:barkode`, `/success/*` → `ref:page:stories`, everything else → `live:page:<id>`. Upload links (`.pdf`, `.docx`) → `{{media_url:live:media:<id>}}`. External links keep their URL; buttons open external links in a new tab with `rel="noopener"`; internal page links never open a new tab. No unknown media or unknown internal link remained after the run (`payload-src/source/fusion-to-blocks-report.json`).
- **Alt text**: file-name-derived alts (e.g. `1000016218`, Facebook `…_n`) are blanked; genuine alts are kept; where the live `<img>` had no alt the media-library alt is used.
- **Headings**: the page title is the only H1 (hero); Fusion "size one" titles become H2; nested `<h1><h2>…` keep the inner level; "headings" longer than 140 characters (Fusion styled paragraphs) become paragraphs.
- **Text left verbatim on purpose** (flag for the client, see `docs/conflict-log.md`): "does not provided funding" (3093); 16-months vs 14-months K9 age (3084); "Retreival" in a registry task line; the 2020 privacy policy wording; "Chris Flemming"/"Chris Fleming" spelling on Meet the Team (person records).

## URL coverage (crawl + sitemap, 2026-09-13)

`tools/url-coverage.mjs` cross-checks this map against a Screaming Frog crawl of the live site (`urls-internal_all.csv`, 350 URLs) and the live page sitemap (`sitemap-post-type-page.xml`, 40 URLs) and writes `docs/reports/url-coverage.md` — one row per URL with its type, live status, post-migration expectation and how that is verified. The numbers below come from that report and from `plugin/heartland-k9s-core/tests/url-coverage-suite.sh`, which replays the migration on the throw-away fresh stack (old site rebuilt with all 40 live pages and 244 attachments at their live ids/paths → plugin + theme from `build/*.zip` → content-only payload with `--adopt-existing`) and fetches every URL.

| URLs | Count | After the migration | Verified by |
|---|---|---|---|
| Indexable HTML pages (19 crawled + 5 sitemap-only: `/privacy-policy/`, `/thank-you/`, `/heartland-obedience-training-2/`, `/success/`, `/success/sample-success-story/`) | 24 | 200 at the same URL (adopted by live id + slug); the two `/success/` stubs are adopted as drafts and 301 → `/stories/` | `tools/url-matrix.sh` rows (main stack: 84 ✅ / 0 ❌, `docs/reports/url-coverage.md`); simulation: 24/24 |
| Registry pages (15 records + master template, sitemap-only) | 16 | `/<slug>/` → 301 (`X-Redirect-By: hk9-legacy`) → `/barkode/<slug>/` (200, `noindex`); master template → `/barkode/` | url-matrix rows; seed list + `redirects.json`; simulation: 16/16 |
| Author archives `/author/admin/`, `/author/hk9director/` | 2 | 301 → `/` — the live behaviour, now a seeded rule (see the row above) | url-matrix rows; `wp hk9 redirects seed` + curl on the main stack; simulation: 2/2 (`X-Redirect-By: hk9-legacy`; `?author=1` → 404) |
| `http://` rows (6 pages, 42 images) | 48 | host-level http→https 301 (LiteSpeed), unchanged; each https target is its own row | — |
| Image URLs under `uploads/<yyyy>/<mm>/` — originals (40), `-scaled` renditions of big originals (48), `-WxH` medium renditions (38) | 126 | 200 at the same URL: adoption never moves, re-uploads or deletes a live file; every URL resolves to a `live:media:<id>` record's `live_path` / `served_full_url` | payload `live_path` match (report rows); simulation: 126/126 → 200 |
| FooGallery cache thumbs `uploads/cache/<yyyy>/<mm>/<name>/<crc>.<ext>` | 84 | kept on disk untouched, no longer referenced once FooGallery is deactivated — each one is a rendition of an attachment listed above; not a content URL | not part of the payload (84/84 absent in the simulation, by design); the new pages reference 799 upload URLs, all resolving, none under `uploads/cache/` |
| Avada theme JS/CSS (44 + 5 `uploads/fusion-styles/`), Fusion Builder/Core, FooGallery, FooBox scripts (18), Cookie Notice (2), `wp-includes` jQuery (2) | 71 | not content URLs: no longer referenced (Cookie Notice and core files are still served) | — |

Total: 371 URLs, none lost. Simulation totals: 34 checks passed, 0 failed (2026-09-13; plugin/theme 1.2.2 from `build/*.zip`, WordPress 7.1, `media_files` adopt 244 / fail 0, `posts_stub` adopt 40); `tools/url-matrix.sh` on the fresh stack: 84 ✅ / 0 ❌.

Notes:

- The author-archive rules were added on 2026-09-13 to `Redirects\Store::seeds()` (installed on plugin activation; `wp hk9 redirects seed` adds them to an already-activated site — verified on the main stack: `/author/admin/` and `/author/hk9director/` → 301 `/` with `X-Redirect-By: hk9-legacy` and `Cache-Control: public, max-age=86400`, `?author=1` and `/author/<unknown>/` stay 404, `?utm_source=` is preserved) and to `payload-src/records/redirects.json`. `payload-lite/` and `build/heartland-k9s-payload-lite.zip` still need a rebuild (`node tools/build-payload.mjs --lite`, `npm run package`) to carry the two records; the packaged plugin 1.2.2 predates the seed, so the suite adds the two rules itself when it finds them missing and says so in the report — a run with a plugin ZIP built from the working tree passed the seeded path (34/34, "seeded by the plugin").
- FooGallery cache thumbs are the only crawled URLs that do not answer on the simulated site: they are not attachments and not in the payload. On the live site they stay where they are (`uploads/cache/` is never touched); their originals — the 244 attachments — are what the new pages render. The `-scaled` files exist on the live site for every big original (WordPress' 2560 px threshold), and the simulation regenerated them from the payload originals at the same names.
- Re-run: `node tools/url-coverage.mjs` regenerates the report from the crawl, the live sitemap and the map (`--probe-live` refreshes the live status of the 21 sitemap-only URLs; the previous simulation numbers are kept); `bash plugin/heartland-k9s-core/tests/url-coverage-suite.sh` repeats the simulation and rewrites the numbers.
